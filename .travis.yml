language: php

php:
  - 8.1

cache:
  directories:
    - $HOME/.composer/cache

before_install:
  - travis_retry composer self-update

install:
  - travis_retry composer install

script:
  - vendor/bin/phpunit
